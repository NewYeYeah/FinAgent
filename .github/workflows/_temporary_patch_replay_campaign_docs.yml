name: temporary-patch-replay-campaign-docs

on:
  push:
    branches: [rt-replay-experiment-campaign-v1]
    paths:
      - "scripts/_patch_replay_campaign_docs.py"
      - ".github/workflows/_temporary_patch_replay_campaign_docs.yml"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rt-replay-experiment-campaign-v1
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python scripts/_patch_replay_campaign_docs.py
      - name: Commit documentation patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/guides/streaming-research-evidence-bridge.md docs/development/changelog.md
          git diff --cached --quiet && exit 0
          git commit -m "Document replay experiment campaign closure"
          git push origin HEAD:rt-replay-experiment-campaign-v1
