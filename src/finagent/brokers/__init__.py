"""Broker integration packages.

Broker adapters are kept separate from deterministic historical execution. Stage-specific
packages must not grow mutation authority before their own acceptance gates permit it.
"""
